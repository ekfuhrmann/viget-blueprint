---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'div'> {}

const { class: className, ...attrs } = Astro.props
---

<div
  class:list={[
    'not-content flex overflow-auto',
    {
      'p-40': !className,
    },
    className,
  ]}
  {...attrs}
>
  <slot />
</div>
