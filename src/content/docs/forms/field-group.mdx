---
title: Field Group
description: A wrapper component used to create a consistent look and feel for form inputs and to provide a consistent way to handle error messages and helper text in forms.
---

import { Tabs, TabItem, Aside, Icon, Code } from '@astrojs/starlight/components'
import Preview from '@/components/starlight/preview/Preview.ts'
import { Image } from 'astro:assets'
import PropTable from '@/components/starlight/PropTable.astro'
import FieldGroup from '@/components/forms/FieldGroup.astro'
import FieldGroupRaw from '@/components/forms/FieldGroup.astro?raw'

Field Group components are used as a wrapper component to create a consistent look and feel for form inputs and to provide a consistent way to handle error messages and helper text. It is suggest that this component be baked into your normal form input component.

<Aside>
  Input field styles are generated via a [Tailwind
  Plugin](https://tailwindcss.com/docs/plugins) which can be found and adapted
  [here](https://github.com/vigetlabs/blueprint-starlight/blob/master/config/tailwind/input.js).
</Aside>

<Preview.Root>
  <Preview.Canvas>
    <FieldGroup
      label="Label"
      name="example-1"
      sublabel="Sub-label content here lorem ipsum"
      instructions="Instruction content goes here lorem ipsum dolor sit."
    >
      <div class="h-40 border-2 border-dashed border-red-500 bg-red-500/20 p-4 text-red-500 before:absolute before:content-['Slot']"></div>
    </FieldGroup>
  </Preview.Canvas>
  <Preview.Docs>
    <Tabs>
      <TabItem label="Astro" icon="astro">
        ```astro
        <FieldGroup
          label="Label"
          name="example-1"
          sublabel="Sub-label content here lorem ipsum"
          instructions="Instruction content goes here lorem ipsum dolor sit."
        >
          <div>Slot</div>
        </FieldGroup>
        ```
      </TabItem>
      <TabItem label="Compiled" icon="seti:html">
        ```html
        <div class="field-group" data-layout="FieldGroup">
          <label for="example-1" id="label-example-1">
            <span class="field-label">Label</span>
            <span class="field-sublabel">Sub-label content here lorem ipsum</span>
          </label>
          <div>Slot</div>
          <span class="field-instructions" id="instructions-example-1">Instruction content goes here lorem ipsum dolor sit.</span>
        </div>
        ```
      </TabItem>
    </Tabs>

  </Preview.Docs>
</Preview.Root>

## API Reference

This component is based on the `<div>` element and can take in any additional HTML attributes. The following are the custom props that can be passed to the component.

<PropTable
  rows={[
    {
      prop: 'name',
      type: 'string',
      description:
        'Unique name of the input field. Should follow a kebab-case naming convention (e.g. "first-name")',
      required: true,
    },
    {
      prop: 'label',
      type: 'string',
      description: 'Visual or assistive label for the input field',
      required: true,
    },
    {
      prop: 'labelHidden',
      type: 'boolean',
      description:
        'Whether to visually hide the label or not. When hidden, the label will still be available to screen readers',
      default: 'false',
    },
    {
      prop: 'sublabel',
      type: 'string',
      description: 'Sub-label content for the input field',
    },
    {
      prop: 'instructions',
      type: 'string',
      description:
        'Instructions for the input field that will be placed below the input',
    },
    {
      prop: 'required',
      type: 'boolean | "long"',
      description:
        'Whether the input field is required or not. If set to "long", the input field will be marked as required and include explicit "required" text.',
      default: 'false',
    },
    {
      prop: 'errorMessages',
      type: 'string[]',
      description: 'Error messages to display below the input field',
    },
  ]}
/>

## Accessibility

The `<FieldGroup>` component includes some accessibility features to help ensure that the component is usable and understandable by all users. The following are some of the key accessibility features that are included in the component and worth considering:

- The `label` element is associated with the input field by using the `for` attribute and includes any sublabel text.
- The `label` and `instructions` elements are associated with the input field by using the `aria-labelledby` attribute. This is an important consideration specifically for the instructions so that they are read out by screen readers when the field is focused, rather than after the input field.
- The `errorMessages` are displayed below the input field and are marked up as a list of error messages.
- The `labelHidden` prop visually hides the label but still ensures it is available to screen readers.
- The `required` prop marks the input field as required and can include explicit “required” text if the value is set to `"long"`.

## Examples

### Required

Use the `required` prop to mark a field as required. You can also use the `required` prop to mark a field as required and include explicit “required” text by passing the value `"long"`.

<Preview.Root>
  <Preview.Canvas>
    <div class="flex flex-wrap gap-24 [&>*]:flex-1">
      <FieldGroup label="Label" name="example-2" required>
        <div class="h-40 border-2 border-dashed border-red-500 bg-red-500/20 p-4 text-red-500 before:absolute before:content-['Slot']"></div>
      </FieldGroup>
      <FieldGroup label="Label" name="example-3" required="long">
        <div class="h-40 border-2 border-dashed border-red-500 bg-red-500/20 p-4 text-red-500 before:absolute before:content-['Slot']"></div>
      </FieldGroup>
    </div>
  </Preview.Canvas>
  <Preview.Docs>
    <Tabs>
      <TabItem label="Astro" icon="astro">
        ```astro 'required' /required="long"/
        <FieldGroup label="Label" name="example-2" required>
          <div>Slot</div>
        </FieldGroup>
        <FieldGroup label="Label" name="example-3" required="long">
          <div>Slot</div>
        </FieldGroup>
        ```
      </TabItem>
      <TabItem label="Compiled" icon="seti:html">
        ```html
        <div class="field-group" data-required data-layout="FieldGroup">
          <label for="example-2" id="label-example-2">
            <span class="field-label">Label</span>
          </label>
          <div>Slot</div>
        </div>
        <div class="field-group" data-required="long" data-layout="FieldGroup">
          <label for="example-2" id="label-example-2">
            <span class="field-label">Label</span>
          </label>
          <div>Slot</div>
        </div>
        ```
      </TabItem>
    </Tabs>
  </Preview.Docs>
</Preview.Root>

### With Helper Text

Use the `sublabel` and `instructions` props to add helper text to the input field.

<Aside>
  You will need to bind the `sublabel` and `instructions` props to the input
  field component you are using by adding an `aria-labelledby` attribute to the
  input field and referencing the `id` of both the `label` and `instructions`
  elements respectively.

See [Text Input](/forms/text-input/) for a working example.

</Aside>

<Preview.Root>
  <Preview.Canvas>
    <FieldGroup
      label="Label"
      name="example-4"
      sublabel="Sublabel lorem ipsum dolor sit amet."
      instructions="Instructions lorem ipsum dolor."
    >
      <div class="h-40 border-2 border-dashed border-red-500 bg-red-500/20 p-4 text-red-500 before:absolute before:content-['Slot']"></div>
    </FieldGroup>
  </Preview.Canvas>
  <Preview.Docs>
    <Tabs>
      <TabItem label="Astro" icon="astro">
        ```astro /sublabel=.*"/ /instructions=.*"/
        <FieldGroup
          label="Label"
          name="example-4"
          sublabel="Sublabel lorem ipsum dolor sit amet."
          instructions="Instructions lorem ipsum dolor."
        >
          <div>Slot</div>
        </FieldGroup>
        ```
      </TabItem>
      <TabItem label="Compiled" icon="seti:html">
        ```html
        <div class="field-group" data-required data-layout="FieldGroup">
          <label for="example-4" id="label-example-4">
            <span class="field-label">Label</span>
            <span class="field-sublabel">
              Sublabel lorem ipsum dolor sit amet.
            </span>
          </label>
          <div>Slot</div>
          <span class="field-instructions" id="instructions-example-4">
            Instructions lorem ipsum dolor.
          </span>
        </div>
        ```
      </TabItem>
    </Tabs>
  </Preview.Docs>
</Preview.Root>

### With Errors

Use the `errorMessages` prop to display error messages below the input field.

<Preview.Root>
  <Preview.Canvas>
    <FieldGroup
      label="Label"
      name="example-5"
      errorMessages={['Error message 1', 'Error message 2']}
    >
      <div class="h-40 border-2 border-dashed border-red-500 bg-red-500/20 p-4 text-red-500 before:absolute before:content-['Slot']"></div>
    </FieldGroup>
  </Preview.Canvas>
  <Preview.Docs>
    <Tabs>
      <TabItem label="Astro" icon="astro">
        ```astro /errorMessages=.*}/
        <FieldGroup
          label="Label"
          name="example-5"
          errorMessages={['Error message 1', 'Error message 2']}
        >
          <div>Slot</div>
        </FieldGroup>
        ```
      </TabItem>
      <TabItem label="Compiled" icon="seti:html">
        ```html
        <div class="field-group" data-state="error" data-layout="FieldGroup">
          <label for="example-5" id="label-example-5">
            <span class="field-label">Label</span>
          </label>
          <div>Slot</div>
          <ul class="field-errors">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="absolute left-0 top-2 size-16 shrink-0"
            >
              <line x1="12" y1="9" x2="12" y2="13"></line>
              <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"></path>
              <line x1="12" y1="17" x2="12.01" y2="17"></line>
            </svg>
            <li>Error message 1</li>
            <li>Error message 2</li>
          </ul>
        </div>
        ```
      </TabItem>
    </Tabs>
  </Preview.Docs>
</Preview.Root>

### With Hidden Label

Use the `labelHidden` prop to visually hide the label. When hidden, the label will still be available to screen readers.

<Preview.Root>
  <Preview.Canvas>
    <FieldGroup label="Label" name="example-6" labelHidden>
      <div class="h-40 border-2 border-dashed border-red-500 bg-red-500/20 p-4 text-red-500 before:absolute before:content-['Slot']"></div>
    </FieldGroup>
  </Preview.Canvas>
  <Preview.Docs>
    <Tabs>
      <TabItem label="Astro" icon="astro">
        ```astro "labelHidden"
        <FieldGroup label="Label" name="example-6" labelHidden>
          <div>Slot</div>
        </FieldGroup>
        ```
      </TabItem>
      <TabItem label="Compiled" icon="seti:html">
        ```html
        <div class="field-group" data-layout="FieldGroup">
          <label for="example-6" id="label-example-6">
            <span class="field-label sr-only">Label</span>
          </label>
          <div>Slot</div>
        </div>
        ```
      </TabItem>
    </Tabs>
  </Preview.Docs>
</Preview.Root>

## Astro Component

It’s recommended that an element like this is made as a reusable component. In this case, we have put together the following Astro component to serve as a blueprint for how you could possibly set up a field group component, and what properties to consider.

<Code code={FieldGroupRaw} lang="astro" title="FieldGroup.astro" />
