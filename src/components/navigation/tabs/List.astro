---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'ul'> {}

const { class: className, ...attrs } = Astro.props
---

<ul
  class:list={[{ 'mb-16 flex list-none border-b p-0': !className }, className]}
  role="tablist"
  {...attrs}
>
  <slot />
</ul>
